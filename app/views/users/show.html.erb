<h1><%= @user.first_name %></h1> 

<p> T'es infos
  <li> Ton prénom : <%= @user.first_name %> </li>
  <li> Ton nom de famille : <%= @user.last_name %> </li>
  <li> Ta description: <%= @user.description %> </li>
  <li> Ton email : <%= @user.email %> </li>
</p>
<%= link_to 'New Event', new_event_path, class: "btn btn-success" %>
<div class="card-columns">
<% @events.each do |event|%>
    <% if event.event_admin_id == @user.id %>
  <div class="card" style="max-width: 18rem;">
    <div class="card-body">
    
      <h5 class="card-title"> Title : <%= event.title %></h5> 
      <p class="card-text"><small class="text-muted"> Author <%= event %></small></p>
      <%= link_to "voir l'event", event_path(event.id), class:"btn btn-primary" %>
      <p class="card-footer"><%= link_to 'Edit', edit_event_path(event), class: "btn" %> <%= link_to 'Destroy', event, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn" %></p>
    </div>
  </div>
  <% end %>
<% end %>
</div>